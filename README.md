# Проект ***"widget_project"***
## Описание:
Проект "widget project"-это проект виджета, который показывает несколько последних успешных банковских операций клиента.

## Установка:
Клонируйте репозиторий:
```
git clone (https://github.com/KonstantinosTsiakkuri/feature-homework_10_1.git)
```
## Тестирование:
добавлены функции тестирования:
1. тестирование модуля  masks
2. тестирование модуля widget
3. тестирование модуля processing
4. добавлены соответствующие фикстуры для каждого теста
## Генераторы:
1. добавлен модуль generators с функциями-генераторами
2. добавлен модуль тестирования test_generators с тестированием функций-генераторов
## Декораторы:
1. Добавлен модуль decorators
2. Добавлено тестирование test_decorators к модулю
## Библиотека csv  и pandas
1. Добавлены модули с функциями, считывающие данные из csv и excel файлов
2. Добавлены модули с тестами для этих функций
## Поиск по регулярным выражениям и подсчёт категорий
1. В модуль `processing` добавлена функция `process_bank_search(data, search)` — поиск транзакций,
   в описании которых встречается заданная строка (регистронезависимо, на основе `re`)
2. В модуль `processing` добавлена функция `process_bank_operations(data, categories)` — подсчёт количества
   операций по категориям (на основе `Counter` из `collections`)
3. Реализована функция `main` в модуле `main` (корень проекта), связывающая всю функциональность проекта:
   выбор источника данных (JSON/CSV/XLSX), фильтрация по статусу операции, сортировка по дате,
   фильтрация по рублёвым транзакциям, поиск по слову в описании, подсчёт операций по категориям
   и вывод итогового списка транзакций в консоль
4. Добавлены тесты к новым функциям и к `main` (`tests/test_processing.py`, `tests/test_main.py`)